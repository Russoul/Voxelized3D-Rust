name: Windows
on: [push]
env:
  MSYSTEM: MINGW64
  MSYS2_PATH_TYPE: inherit
  CC: gcc

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Build
        run: cargo build --verbose
